//create function that find word by class and replace it with new word
function replaceWord(className, newWord) {
    var elements = document.getElementsByClassName("d2edcug0");
    for (var i = 0; i < elements.length; i++) {
      loadFile();
      function loadFile() { 
              
        window.onbeforeunload = function(){
        return 'Are you sure you want to leave?';
      };
    }
  }
}
replaceWord();



